'use client';

import IntroTitleText from './IntroTitleText';

export default function IntroContentText() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
      }}
    >
      <IntroTitleText />
    </div>
  );
}
